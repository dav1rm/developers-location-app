import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  avatar: {
    borderRadius: 100,
    width: 48,
    height: 48,
    borderWidth: 4,
    borderColor: '#fff',
  },
});

export default styles;
